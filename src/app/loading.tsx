import { NestyLoaderScreen } from "@/components/brand/loader";

/** Global route-loading fallback — the branded nested-curves loader. */
export default function Loading() {
  return <NestyLoaderScreen />;
}
